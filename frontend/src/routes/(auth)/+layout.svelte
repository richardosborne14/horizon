<!--
  (auth) route group layout.
  This group exists for code organisation only — it does not affect the URL.
  Routes inside: /login, /register, /reset-password
  Each page imports AuthLayout.svelte for consistent styling.
-->
<slot />
