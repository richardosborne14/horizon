# Horizon — Learnings & Gotchas

Updated after every task. Check this before starting work.

| # | Area | Gotcha | Solution |
|---|------|--------|----------|
| 1 | Git | ComCoi code must never be pushed to public Horizon repo | Renamed `origin` → `comcoi` (protected). New `origin` = `https://github.com/richardosborne14/horizon.git`. First push only after TASK-0.2 + TASK-0.3 complete. |
| 31 | Sprint 5 | `_compute_investment_growth` skips vehicles with `monthly <= 0`. Tax-by-holding-period tests must use `monthly=1` to exercise the tax path. | Use small positive monthly values in unit tests targeting tax-rate selection. |
| 32 | Sprint 5 | `compute_readiness_score` without a goal excludes `goal_coverage` component and reweights remaining 5 components to sum to 100. | Always pass `monthly_revenue_goal` if available; otherwise the caller gets a score based on savings/buffer/diversification only. |
| 33 | Sprint 5 | AreaChart SVG uses margins (LEFT=50px, RIGHT=10px) to reserve space for Y-axis labels and goal-line text. `preserveAspectRatio="xMidYMid meet"` prevents distortion. | Keep margins in sync with Y-axis label width. Use `formatY` prop to allow different formatters per chart (wealth in k€, income in €). |
| 34 | Sprint 5 | fpdf2 added to requirements.txt for PDF export. Fonts are built-in (Helvetica, Courier) — no TTF embedding needed for MVP. | PDF endpoint is at `/api/projection/export`. 3-page document: exec summary, detailed table, configuration. |
| 2 | Naming | Project renamed from "Horizon 30" → "Horizon" | Applied everywhere: task docs, prototype filename (`horizon.jsx`), reference doc, git repo URL. No "30" suffix. |
| 3 | Prototype | Prototype lives at `dev-docs/prototype/horizon.jsx` | 954-line React prototype is the canonical UX reference. Reference doc at `dev-docs/PROTOTYPE_REFERENCE.md` maps every section to planned routes/components. |
| 4 | Task docs | dev-docs 2 task docs reference "Horizon 30" throughout | Historical — written before the rename to "Horizon". Read as "Horizon" when executing. The substance (requirements, acceptance criteria) is unchanged. |
| 5 | Sprint 0 | DB has 43 tables (full ComCoi schema) — TASK-0.2 backstrip only removed code references, not tables | Sprint 0 Definition of Done requires "creates only: users, admin_config, stripe_events_processed". Orphaned tables don't cause errors (backend only references kept models) but should be cleaned. Deferred: create fresh baseline migration in Sprint 1. |
| 6 | Sprint 0 | `.env` file was missing on first `docker compose up` | Created `.env` with dev defaults. Settings class has sensible defaults so most values can be empty in dev. |
| 7 | Sprint 0 | `PROTOTYPE_REFERENCE.md` was missing until Sprint 0 closure | Created from TASK-0.6 spec with full section mapping, 15 design patterns, color semantics, typography tables. Now referenced by `.clinerules`. |
| 8 | Docker | Stale ComCoi containers held ports 47432/47002/47178 | Removed with `docker rm -f $(docker ps -aq --filter "name=horizon")`. Each project keeps its own compose project name. |
| 9 | Frontend | ComCoi salon text remains in a few public route files | TASK-0.3 defers full public-page retheme. These are non-`(app)/` routes — clean in Sprint 1 landing page rebuild. |
| 10 | Sprint 1 | Backend: auth dependency is `get_current_user` (returns User), not `get_current_user_id` | Use `current_user: User = Depends(get_current_user)` then `current_user.id`. |
| 11 | Sprint 1 | Backend: DB dependency is `get_db`, not `get_async_session` | Use `db: AsyncSession = Depends(get_db)`. |
| 12 | Sprint 1 | Alembic autogenerate detects phantom table drops for removed ComCoi models | Delete autogenerated migration. Write minimal migration by hand. Set `down_revision = '046'`. |
| 13 | Sprint 1 | SvelteKit uses legacy mode (`export let`), not runes (`$props`) — using `$state` triggers runes detection | Use Svelte 4 syntax: `export let`, `let x =`, `$:`. Never use `$state()`, `$props()`. |
| 14 | Sprint 1 | ARCHITECTURE.md does not exist in codebase | Document architecture inline in LEARNINGS.md until created. |
| 15 | Sprint 1 | Inflation preview decimal values must match actual computation, not hand-math | Compute expected values with actual Decimal arithmetic. Trust `quantize()`. |
| 16 | Sprint 2 | LifeEntity JSONB cost_events array — updates replace entire array | Send full array on PUT. Handle Pydantic CostEvent models separately from `model_dump()` (exclude cost_events, convert manually). |
| 17 | Sprint 2 | Amount floats in JSONB lose Decimal precision (600.0 vs 600.00) | Use `Decimal(str(value)).quantize(Decimal("0.01"))` when deserializing from JSONB. |
| 18 | Sprint 2 | September rule: Oct 1 baby → `(2029-09-01 - 2025-10-01).days // 365 = 3` not 4 | Integer division gives 3. Both 3 and 4 are valid outcomes depending on birth date. |
| 19 | Sprint 2 | Alembic revision IDs must be globally unique across all migration files | Use fresh UUID-derived IDs (`uuid4()[:12]`) to avoid "Revision present more than once" errors. |
| 20 | Sprint 2 | Svelte 4 invalid self-closing tags: `<span .../>` must be `<span ...></span>` | Use explicit closing tags for non-void HTML elements in Svelte. |
| 21 | Sprint 3 | `Decimal("0")` serializes as `"0"` in JSON (not `"0.00"`) via FastAPI's JSON encoder. | Use `in ("0", "0.00")` in test assertions for zero values. Quantize P&L values with `Decimal("0.01")`. |
| 22 | Sprint 3 | GET by ID must filter on `is_active == True` for soft-deletes to return 404. | Add `.where(Model.is_active == True)` to both list and get-by-ID queries. |
| 23 | Sprint 4 | `sum(balances.values())` on an empty dict returns `int 0`, not `Decimal(0)` — `.quantize()` then fails. | Use `sum(balances.values(), Decimal("0"))` to force Decimal accumulator. |
| 24 | Sprint 4 | `str(Decimal("1550"))` produces `"1550"`, not `"1550.00"` — quantize before string conversion only helps if called explicitly. | In test assertions for summary values, use `in ("1550", "1550.00")` to handle both representations. |
| 25 | Sprint 4 | Pessimistic scale does not always produce lower wealth than optimistic — investment ceilings grow with inflation, and higher inflation raises the cap faster. | Test that scales differ (`!=`) rather than assuming a directional inequality. |
| 26 | Sprint 4 | Life entity cost events inflate at `inflation_rate`, not `cost_living_rate`. Test expectations must use the same rate the engine uses. | Document the distinction clearly: base expenses use cost_living, life entities use inflation. |
| 27 | Sprint 5 | Post-retirement phase adds 25 years by default to the projection timeline. Existing tests that assert `len(timeline) == 30` must pass `post_retirement_years=0` to preserve backward compatibility. | Add `post_retirement_years=0` to all test fixtures. The engine stops early if wealth exhausts. |
| 28 | Sprint 5 | Svelte 4 `{@const}` can only be used as immediate child of `{#each}`, `{#if}`, etc. — cannot be placed inside `<tbody>` or arbitrary HTML elements. | Inline the expression directly instead of using `{@const}`. |
| 29 | Sprint 5 | `goal_reached` was computed with `total_monthly_income` which includes work salary — incorrectly flagging "objectif atteint" for anyone whose current salary exceeded the goal. Since salary drops to zero at retirement, the goal must use retirement-relevant income only. | Use `retirement_monthly_income = passive + (proj_inc + pension) / 12`. Excludes work salary and CAF (both stop at retirement). Updated `test_goal_detection` target from 4000 to 2000. |
| 30 | Sprint 5 | `RecurringExpenseCreate.label` requires `min_length=1` but the frontend `addRecurring()` sent `label: ''` (empty string), causing 422 on the Vie page. | Changed default to `'Nouvelle depense'` and added guard in `debouncedSaveRecurring` to skip update when label is empty. |
| 31 | Sprint 5 | `abs(sum(...), Decimal("0"))` is a TypeError — Python's `abs()` takes exactly 1 argument. | Use `abs(sum(...))` with fallback `or Decimal("1")` on the result. |
| 35 | Sprint 5 | Pydantic models with cross-references must be defined in dependency order. `CompareResponse` references `ProjectionResponse` which must be defined first. | Place reference schemas after the ones they depend on, or use `from __future__ import annotations` + string forward references. |
| 36 | Sprint 5 | Scenario comparison `POST /api/projection/compare` runs the engine twice — once for base, once with overrides. Clone input with `deepcopy` before mutating. | Override fields: `monthly_savings`, `target_retirement_age`, `growth_rate`, `monthly_expenses_delta`, `disable_project`, `extra_monthly_investment`. Savings distributed proportionally across existing allocations. |
| 37 | Sprint 5 | PDF export table rows must handle both `YearProjection` dataclass and dict types. Use `getattr(t, field, default)` + `t.get(field, default)` fallback for dicts. | The `_v()` helper extracts fields safely. Previous code duplicated row construction leading to bugs. |
| 38 | Sprint 5 | AreaChart X-axis labels were rendering indices instead of years. The data now carries `year` fields from the runway page. | `computeXLabels` reads `vals[idx]?.year`; falls back to index if year is not present. |
| 39 | Sprint 5 | TASK-5.8 (Onboarding Flow) deferred to Sprint 6 — the welcome overlay, section progress bar, and completion toasts are P2 polish that didn't block core value delivery. | Existing onboarding handles business setup. Deferred items documented in Sprint 5 plan. |
| 40 | Sprint 6 | Car defaults now use `to_age=99` for ongoing costs (insurance, fuel, maintenance) and pre-generate replacement events through age 40. Existing old cars no longer silently contribute 0€. Expired entity detection added to `LifeEntityRead` schema. | Old tests referencing `c-replace` event ID must use `c-replace-1` (rolling model uses numbered replacements). |
| 41 | Sprint 6 | `YearProjection` has a new `loan_expenses` field (fixed nominal, not inflation-adjusted). Loans are stored in the new `loans` table with end dates. Projection engine extracts loans from `ProjectionInput.loans` list. | `_compute_loan_expenses()` handles both `date` objects and ISO strings for start_date/end_date. Loans with no end_date are treated as permanent (fallback for flat credit field). |
| 42 | Sprint 6 | Career history stored in `career_periods` table. Pension engine v2 (`estimate_monthly_pension_v2`) uses full career (salaried + AE + unemployment + parental leave) to compute trimestres and SAM. | Register new routers in `backend/app/routers/__init__.py` — the system uses a single `api_router` with `include_router()`. PASS inflates over time in SAM calculation — compare against inflated PASS, not static 2024 PASS. |
| 43 | Sprint 6 | Alembic autogenerate still produces phantom table drops for ComCoi legacy tables (same as LEARNINGS #12). TASK-6.1 migration needed the same hand-write treatment. | Hand-write minimal migration that only creates the new `career_periods` table. Delete autogenerated migration, use `op.create_table()` directly. |
| 44 | Sprint 6 | `NetWorthSnapshot` uses upsert pattern (single row per user). GET returns virtual default when no row exists — no 404. | The `net_worth_snapshots` table stores non-investment/non-loan assets. Summary endpoint aggregates all three sources. `extra_liquid` parameter added to `compute_readiness_score()` and `_compute_buffer_adequacy()` to include cash reserves. |
| 45 | Sprint 6 | `GET /api/projection/expense-timeline` (TASK-6.6) computes year-by-year expense breakdown + key lifecycle events from the projection engine. Expenses are converted to monthly values. `_detect_expense_events()` identifies loan terminations, kid independence, pet EOL, car replacements. | Schema: `ExpenseTimelineResponse` wraps `timeline` (list of `ExpenseTimelineYear`) + `key_events` (list of `ExpenseTimelineEvent`). Import `ExpenseTimelineResponse` at module level for the decorator; `ExpenseTimelineYear`/`ExpenseTimelineEvent` can be imported inside the function body. |
| 46 | Sprint 6 | `run_sensitivity_analysis()` (TASK-6.7) clones `ProjectionInput`, applies 6 parameter nudges (savings, expenses, growth, retirement age, PEA redirect, loan freed), and re-runs projection for each. Results ranked by abs(delta_wealth). | `_apply_nudge()` deep-copies input, applies nudge, returns modified input. Endpoint `GET /api/projection/sensitivity` returns `SensitivityResponse`. Tests in `test_task_6_7_sensitivity.py`. |
| 47 | Sprint 6 | `GET /api/profile/waterfall` (TASK-6.8) assembles income/expense flow for current year. Uses `_assemble_input` from projection router and `project_timeline()` for year 0. | `WaterfallResponse` has `monthly` and `annual` breakdowns plus status (surplus/deficit/breakeven). Schemas in `schemas/profile.py`. Tests in `test_task_6_8_waterfall.py`. |
| 48 | Sprint 6 | `generate_lifecycle_alerts()` (TASK-6.9) produces time-specific alerts: loan termination, kid independence, car replacement (2yr), ceiling approaching, retirement countdown, pet EOL, expense peak. Max 3 per year, 20 total. | `LifecycleAlert` dataclass with id, alert_type, year, severity, impact. Endpoint `GET /api/projection/alerts`. Schemas in `schemas/projection.py`. Tests in `test_task_6_9_lifecycle_alerts.py`. |
| 49 | Sprint 6 | `GET /api/projection/year/{year}` (TASK-6.10) returns full drill-down: income sources, charges, expenses with inflation factor, life entity cost events, loan status, investment per-vehicle contributions/returns/balances. Auto-generated explanation. | `YearDrillDownResponse` aggregates ~10 sub-schemas. Extracts from cached timeline (no re-computation needed). Life entity cost events inflated at the year's inflation factor. |
| | 50 | Sprint 6 | Frontend: All 10 Sprint 6 tasks have frontend components now. T-6.4 (car lifecycle — Vie page amber warning + extend), T-6.3 (loans — Expenses page CRUD), T-6.1 (career — Identity page timeline), T-6.2 (pension — Runway card), T-6.5 (net worth — Runway card), T-6.6 (expense timeline — Expenses page stacked bars + events), T-6.7 (sensitivity — Runway horizontal bars), T-6.8 (waterfall — Revenue page flow chart), T-6.9 (alerts — Runway chronological feed), T-6.10 (drill-down — Runway click expansion). | Frontend uses existing API endpoints, adds server-side data fetching via `+page.server.ts` (`fetchWithAuth` pattern), renders in respective Svelte pages using Card component, `fmt`/`fmtK`/`fmtDec` formatters, design tokens. Confidence: 9/10. |
